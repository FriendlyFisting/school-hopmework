//void collision(){
// if(dist(Bullet.xpos, Bullet.ypos, bat.get(1).xpos, tanks.get(1).ypos) < tankHitBox) && winnerFound == false) {  //Remove bullet and add to player 2's hit score, whenever the distance between a bullet and tank goes below a limit. This also only works when the win state is still false
  //    bullets.remove(i);
   //   tanks.get(0).hitCount++;
   // }) {
   
 
